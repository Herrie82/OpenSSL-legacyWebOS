.DS_Store
ipks/_build_*
ipks/_b_*
# anchored: ignores only the legacy top-level topaz binary, NOT devices/<dev>/BrowserServer.bin
# (unanchored, this pattern matched at any depth and silently excluded every device's stock bin)
/BrowserServer.bin
ipks-backup/
analysis/
